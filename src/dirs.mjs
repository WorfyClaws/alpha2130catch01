import path from 'path';

const basedir = path.join('.', 'data');

export const imageDir = path.join(basedir, 'images');
export const modelDir = path.join(basedir, 'model');
export const testsDir = path.join(basedir, 'tests');

