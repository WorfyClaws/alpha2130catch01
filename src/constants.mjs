import path from 'path';

const basedir = path.join('.', 'data');

export const bulbapediaImageDir = path.join(basedir, 'bulbapedia');
export const pokecordImageDir = path.join(basedir, 'pokecord');
export const trainingImageDir = path.join(basedir, 'training');
export const modelDir = path.join(basedir, 'model');
